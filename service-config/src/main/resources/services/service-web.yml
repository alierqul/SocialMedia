server:
  port: 80

spring:
  zipkin:
    enabled: true
    base-url: http://34.118.1.180:9411/
    service-name: service-web
logging:
  level:
    root: warn
    org.springframework.boot: info
    com.bilgeadam: debug
  file:
    name: log-service-web.log

s3:
  accesskey: ${AWS_ACCESS_KEY}
  secretkey: ${AWS_SECRET_KEY}
